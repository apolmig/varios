# Pulse Atlas

A lightweight, end-to-end prototype for a Palantir-style conflict observatory. It renders a
strategic overview of global conflict theaters with filters, risk metrics, and a live narrative
feed.

## Run locally

Open `src/index.html` in a browser. The app uses Leaflet via CDN, so it works without a build
step.
